World:
	CrateSpawner:
		CheckboxEnabled: False
		CheckboxLocked: True
	-SpawnMPUnits:
	-MPStartLocations:
	ObjectivesPanel:
		PanelName: MISSION_OBJECTIVES
	MapBuildRadius:
		AllyBuildRadiusCheckboxLocked: True
		AllyBuildRadiusCheckboxEnabled: False
		BuildRadiusCheckboxLocked: True
		BuildRadiusCheckboxEnabled: True
	MapOptions:
		TechLevelDropdownLocked: True
		ShortGameCheckboxLocked: True
		ShortGameCheckboxEnabled: False
	LuaScript:
		Scripts: towm4.lua
	MissionData:
		Briefing: With the construction of each of the Stabilizer devices nearing completion, some of the planet's commanding officers have not been keen on seeing the devices to said completion. The Yellow Comet army has deployed two bases near the first Stabilizer device. these are clearly a threat and should be eliminated. We will leave you to it, Commander. Take control and send the Yellow Comet packing.

Player:
	-ConquestVictoryConditions:
	MissionObjectives:
		EarlyGameOver: true
	Shroud:
		FogCheckboxLocked: True
		FogCheckboxEnabled: True
		ExploredMapCheckboxLocked: True
		ExploredMapCheckboxEnabled: False
	PlayerResources:
		DefaultCashDropdownLocked: True
		DefaultCash: 6000
	ModularBot@CampaignAI:
		Name: Campaign Player AI
		Type: campaign

SCSCIENCEFACILITY:
	Buildable:
		Prerequisites: ~disabled

SCGHOST:
	Buildable:
		Prerequisites: ~disabled

SCSIEGETANK:
	Buildable:
		Prerequisites: ~disabled

SCTTRUCK:
	Buildable:
		Prerequisites: ~disabled

SCTMCV:
	Buildable:
		Prerequisites: ~disabled

SCBATTLECRUISER:
	Buildable:
		Prerequisites: ~disabled

awsilo:
	Buildable:
		Prerequisites: ~disabled